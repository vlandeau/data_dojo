To install the project, please install [graphviz](https://graphviz.org/download/) and [poetry](https://python-poetry.org/docs/#installation) then run `poetry install`.

To launch, run `make notebooks` or `make jupyterlab`
